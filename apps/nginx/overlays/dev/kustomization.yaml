apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: maisfit-dev

resources:
  - ../../base
  - middleware.yaml
  - ingressroute.yaml

patches:
  - path: configmap-patch.yaml
    target:
      kind: ConfigMap
      name: nginx-index
